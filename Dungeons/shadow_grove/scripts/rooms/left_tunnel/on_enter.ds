$m1 = "the tunnel is an overgrown passage leading from the `entrance` to the `shrine`. Held into the wall is an old skeleton wearing pit of rotted armor"


if (not <.rusty_sword.picked_up>) {
    output(
        $m1.."."::""
    )
} else {
    output(
        $m1.."and clutching a rusted `sword`."
    )
}
