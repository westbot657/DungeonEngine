
if (<#enemy.health> <= <target_health>) {
    [engine:combat/trigger](
        [engine:text/join](
            <#enemy.uid>,
            "health-check",
            <target_health>,
            seperator="-"
        )
    )
}
