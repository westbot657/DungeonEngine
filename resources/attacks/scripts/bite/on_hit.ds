
output(
    [engine:random/choice](%[
        join("`", <#enemy.name>,"` bites `",<target.name>,"`, dealing ",<damage>," damage",seperator=""),
        join("`", <#enemy.name>,"` takes a bite at `",<target.name>,"` and deals ",<damage>," damage",seperator="")
    ])
)
