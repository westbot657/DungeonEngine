
[engine:combat/message](
    [engine:random/choice](%[
        [engine:text/join](<#enemy.name>,"bites",<target.name>,"but doesn't do any damage"),
        [engine:text/join](<#enemy.name>,"takes a bite at",<target.name>,"but misses")
    ])
)
