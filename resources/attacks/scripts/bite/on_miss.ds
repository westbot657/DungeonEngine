
output(
    [engine:random/choice](%[
        <#enemy.name>.."bites"..<target.name>.."but doesn't do any damage",
        <#enemy.name>.."takes a bite at"..<target.name>.."but misses"
    ])
)

#!~/attacks/bite
#!attack-miss-script
output(
    random.choice([
        #enemy.name.."bites"..#target.name.."but doesn't do any damage",
        #enemy.name.."takes a bite at"..#target.name.."but misses"
    ])
)
