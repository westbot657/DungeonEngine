

$macro($arg1, $arg2) {
    $arg1

    $arg2

    output(<x>)
}



$macro(<v> = 5, {output("hi") <x> = input()})

if (<.blocked>) {
    if (<.blocked_message>) {
        output(<.blocked_message>)
    }
} else {
    if (<.travel_message>) {
        output(<.travel_message>)
    }
    [engine:player/set_location](<.target>)
}